# 内置服务器功能说明

## 概述

AircraftWar 应用已经集成了**内置HTTP服务器**，可以在应用启动时自动运行，无需外部服务器即可进行多人联机对战。

## 功能特性

### ✅ 自动启动
- 应用启动时，服务器会**自动在后台启动**
- 默认监听端口：**8080**
- 服务器地址：**localhost** 或 **127.0.0.1**

### ✅ 完整的对战服务
内置服务器提供完整的多人对战API：
- 创建房间
- 加入房间
- 实时分数同步
- 对战状态管理
- 房间管理

### ✅ 用户友好的UI
多人对战大厅界面提供：
- **服务器状态实时显示**（运行中/未运行）
- **"使用本地"快捷按钮**：一键切换到本地服务器
- 自动检测服务器状态并提示启动

## 使用方法

### 方式一：使用本地服务器（推荐）

1. 打开应用，进入"多人对战大厅"
2. 查看服务器状态：
   - ✅ **本地服务器运行中 (localhost:8080)** - 可以直接使用
   - ⚠️ **本地服务器未运行** - 需要启动
3. 点击 **"📱 使用本地"** 按钮
4. 如果服务器未运行，会提示是否启动
5. 服务器地址自动填充为 `localhost`
6. 创建或加入房间开始游戏

### 方式二：连接外部服务器

1. 在"服务器地址"输入框中输入外部服务器IP
2. 例如：`192.168.1.100`（局域网）或 `10.0.2.2`（模拟器）
3. 创建或加入房间

## 技术实现

### 核心组件

#### 1. **EmbeddedBattleServer**
- 位置：`app/src/main/java/edu/hitsz/server/EmbeddedBattleServer.java`
- 功能：完整的HTTP服务器实现
- 特性：
  - 多线程处理客户端请求
  - RESTful API设计
  - 房间管理和状态同步

#### 2. **ServerManager**
- 位置：`app/src/main/java/edu/hitsz/server/ServerManager.java`
- 功能：服务器生命周期管理
- 特性：
  - 单例模式
  - 自动启动配置
  - 状态查询接口

#### 3. **AircraftWarApplication**
- 位置：`app/src/main/java/edu/hitsz/AircraftWarApplication.java`
- 功能：应用启动时初始化服务器
- 配置：在 `AndroidManifest.xml` 中声明

### API端点

服务器提供以下HTTP API：

| 端点 | 方法 | 功能 |
|------|------|------|
| `/battle/create` | POST | 创建房间 |
| `/battle/join` | POST | 加入房间 |
| `/battle/update` | POST | 更新分数 |
| `/battle/status` | GET | 查询状态 |
| `/battle/end` | POST | 结束对战 |

## 配置选项

### 自动启动设置

服务器默认**自动启动**。如需修改：

```java
ServerManager serverManager = ServerManager.getInstance(context);
serverManager.setAutoStart(false); // 禁用自动启动
```

### 手动控制服务器

```java
ServerManager serverManager = ServerManager.getInstance(context);

// 启动服务器
serverManager.startServer();

// 停止服务器
serverManager.stopServer();

// 检查状态
boolean isRunning = serverManager.isServerRunning();
```

## 网络权限

应用已配置必要的网络权限（在 `AndroidManifest.xml` 中）：

```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
```

## 使用场景

### 场景1：单设备测试
- 在同一设备上运行服务器和客户端
- 使用 `localhost` 连接
- 适合开发和调试

### 场景2：局域网对战
- 一台设备运行服务器
- 其他设备通过局域网IP连接
- 例如：`192.168.1.100:8080`

### 场景3：模拟器测试
- 模拟器使用 `10.0.2.2` 连接主机
- 主机运行服务器
- 适合开发环境测试

## 优势

✅ **无需外部依赖**：不需要单独部署服务器  
✅ **即开即用**：应用启动即可联机  
✅ **简化部署**：一个APK包含所有功能  
✅ **降低延迟**：本地通信速度更快  
✅ **离线可用**：无需互联网连接  

## 注意事项

⚠️ **端口占用**：确保8080端口未被其他应用占用  
⚠️ **防火墙**：局域网对战需要允许应用通过防火墙  
⚠️ **性能**：移动设备性能有限，建议房间数量不超过10个  

## 故障排查

### 问题：服务器无法启动
- 检查8080端口是否被占用
- 查看Logcat日志：`adb logcat | grep ServerManager`
- 尝试重启应用

### 问题：无法连接到本地服务器
- 确认服务器状态显示为"运行中"
- 使用 `localhost` 而不是 `127.0.0.1`
- 检查网络权限是否授予

### 问题：局域网无法连接
- 确认两台设备在同一网络
- 使用正确的局域网IP地址
- 检查防火墙设置

## 更新日志

### v2.1 (2026-05-11)
- ✅ 修复AndroidManifest配置，启用自动启动
- ✅ 添加服务器状态实时显示
- ✅ 添加"使用本地"快捷按钮
- ✅ 优化用户体验和提示信息

### v2.0
- ✅ 集成内置HTTP服务器
- ✅ 实现完整的对战API
- ✅ 支持自动启动配置

## 开发者信息

如需修改服务器端口或其他配置，请编辑：
- `EmbeddedBattleServer.java` - 修改 `PORT` 常量
- `ServerManager.java` - 修改自动启动逻辑

---

**享受无缝的多人对战体验！** 🎮✈️
