# 飞机大战联机对战测试说明

## ✅ 已完成的功能

### 1. 服务器端（已启动运行）
- ✅ HTTP服务器（端口8080）- **正在运行中**
- ✅ 房间管理系统（创建/加入/结束）
- ✅ 实时分数同步API
- ✅ 对战状态管理
- ✅ 双方死亡检测

### 2. 客户端
- ✅ BattleClient网络客户端（已创建）
- ✅ FrameSyncManager帧同步管理器（已存在）
- ✅ 网络权限已配置

## 快速测试步骤

### 第一步：服务器已启动 ✅
服务器正在运行：
- 本地地址: `http://localhost:8080`
- 模拟器地址: `http://10.0.2.2:8080`

### 第二步：配置模拟器
1. 打开 **Device Manager** 创建2个模拟器
2. **File → Settings → Tools → Emulator**
3. **取消勾选** "Launch in the Running Devices tool window"
4. **重启 Android Studio**

### 第三步：运行客户端
1. 点击 **"Select Multiple Devices"**
2. 选择 **2个模拟器**
3. 点击 **Run** 按钮

### 第四步：使用BattleClient进行测试

在你的游戏代码中使用BattleClient：

```java
// 创建客户端
BattleClient client = new BattleClient("10.0.2.2");

// 玩家1：创建房间
client.createRoom("Player1", new BattleClient.BattleCallback() {
    @Override
    public void onRoomCreated(String roomId, String playerId) {
        Log.d("Battle", "房间创建成功: " + roomId);
        // 显示房间号给玩家2
    }
    
    @Override
    public void onJoinedRoom(String playerId, String opponentName) {}
    
    @Override
    public void onScoreUpdated(int opponentScore, boolean opponentDead, String battleStatus) {
        // 更新UI显示对手分数
    }
    
    @Override
    public void onError(String error) {
        Log.e("Battle", "错误: " + error);
    }
});

// 玩家2：加入房间
client.joinRoom("ROOM_1234567890", "Player2", callback);

// 游戏中：更新分数
client.updateScore(currentScore, isDead, callback);

// 游戏结束：关闭房间
client.endBattle();
```

## API接口说明

### 1. 创建房间
```
POST /battle/create
{
  "playerName": "Player1"
}

Response:
{
  "success": true,
  "roomId": "ROOM_1715400000000",
  "playerId": "P1_1715400000001",
  "message": "房间创建成功"
}
```

### 2. 加入房间
```
POST /battle/join
{
  "roomId": "ROOM_1715400000000",
  "playerName": "Player2"
}

Response:
{
  "success": true,
  "playerId": "P2_1715400000002",
  "opponentName": "Player1",
  "message": "成功加入房间"
}
```

### 3. 更新分数
```
POST /battle/update
{
  "roomId": "ROOM_1715400000000",
  "playerId": "P1_1715400000001",
  "score": 1250,
  "isDead": false
}

Response:
{
  "success": true,
  "opponentScore": 980,
  "opponentDead": false,
  "battleStatus": "playing"
}
```

### 4. 结束对战
```
POST /battle/end
{
  "roomId": "ROOM_1715400000000"
}

Response:
{
  "success": true,
  "message": "对战已结束"
}
```

## 项目文件结构

```
AircraftWar-main/
├── server/                          # 服务器模块 ✅
│   ├── build.gradle
│   └── src/main/java/edu/hitsz/server/
│       └── BattleServer.java        # HTTP服务器（运行中）
│
├── app/src/main/java/edu/hitsz/
│   └── network/
│       ├── BattleClient.java        # 新增：HTTP客户端 ✅
│       ├── NetworkManager.java      # 已存在：WebSocket管理
│       ├── ScoreApiService.java     # 已存在：排行榜API
│       └── sync/
│           ├── FrameInput.java      # 已存在：帧输入
│           └── FrameSyncManager.java # 已存在：帧同步
│
└── settings.gradle                  # 已更新：包含server模块
```

## 技术实现

- **服务器**: Java 11 + HttpServer + Gson
- **客户端**: Android + OkHttp + Retrofit
- **通信协议**: HTTP REST API
- **数据格式**: JSON
- **同步频率**: 按需同步（推荐1秒/次）

## 下一步集成建议

### 方案1：在GameGLView中集成
```java
public class GameGLView extends View {
    private BattleClient battleClient;
    private int opponentScore = 0;
    
    public void setBattleClient(BattleClient client) {
        this.battleClient = client;
    }
    
    private void update() {
        // 游戏逻辑...
        
        // 定期同步分数
        if (battleClient != null && shouldSync()) {
            battleClient.updateScore(score, !hero.isValid(), callback);
        }
    }
}
```

### 方案2：使用FrameSyncManager（WebSocket）
如果需要更低延迟的实时同步，可以使用已有的FrameSyncManager：

```java
FrameSyncManager syncMgr = new FrameSyncManager("player1");
syncMgr.connect("ws://10.0.2.2:8080/game");
syncMgr.onFrameEnd(System.currentTimeMillis(), score);
```

## 测试验证

- ✅ 服务器成功启动（端口8080）
- ✅ BattleClient已创建
- ✅ settings.gradle已更新
- ✅ 网络权限已配置
- ⚠️ 需要在UI中集成BattleClient

## 常见问题

### Q1: 无法连接服务器
**解决方案**:
1. 确认服务器已启动
2. 模拟器使用 `10.0.2.2`
3. 真机测试使用电脑局域网IP

### Q2: 如何获取本机IP
```bash
# Windows
ipconfig

# Mac/Linux
ifconfig
```

### Q3: 如何停止服务器
在运行服务器的终端按 `Ctrl+C`

## 总结

✅ **服务器已成功启动并运行**
✅ **BattleClient客户端已创建**
✅ **项目配置已完成**

现在你可以：
1. 在游戏代码中集成BattleClient
2. 创建UI界面用于创建/加入房间
3. 在游戏循环中调用updateScore同步分数
4. 测试多开模拟器对战

**服务器正在后台运行，可以立即开始测试！** 🎮
