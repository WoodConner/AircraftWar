# 联机对战使用说明

## 问题修复总结

已修复以下问题：
1. ✅ 端口号设置不生效
2. ✅ 创建房间失败
3. ✅ 加入房间失败
4. ✅ 容易卡死

## 正确使用流程

### 设备A（创建房间）：

1. **启动服务器**
   - 打开应用，进入"多人对战大厅"
   - 设置端口号（如 8080）
   - 点击"📱 使用本地"按钮
   - 点击"启动"
   - 等待提示"✅ 服务器已启动"

2. **创建房间**
   - 确认服务器状态显示"✅ 本地服务器运行中 (端口:8080)"
   - 点击"🎮 创建房间"
   - 记下房间ID（如 ROOM_1778492020802）
   - 等待对手加入

3. **开始对战**
   - 当看到"✅ 对手已加入！"
   - 点击"⚔️ 开始对战"

### 设备B（加入房间）：

1. **设置服务器地址**
   - 服务器地址：`10.0.2.2`（模拟器必须用这个）
   - 端口号：`8080`（与设备A一致）

2. **加入房间**
   - 输入设备A提供的房间ID
   - 点击"🚪 加入房间"
   - 等待提示"已加入房间"

3. **开始对战**
   - 点击"⚔️ 开始对战"

## 重要提示

### 模拟器联机
- ⚠️ 所有模拟器都必须使用 `10.0.2.2` 作为服务器地址
- ❌ 不能使用 `10.0.2.15` 或 `10.0.2.16`
- ✅ 端口号必须一致（如都用 8080）

### 端口设置
- 只在【一个】设备上启动服务器
- 所有设备使用相同的端口号
- 如果端口被占用，尝试其他端口（如 9090）

### 常见错误

**错误1：创建房间失败**
- 原因：服务器未启动
- 解决：先点击"使用本地"启动服务器

**错误2：加入房间失败**
- 原因：服务器地址或端口号错误
- 解决：检查地址是否为 10.0.2.2，端口是否一致

**错误3：连接超时**
- 原因：网络问题或服务器未响应
- 解决：重启服务器，检查防火墙设置

## 修改内容

### 1. ServerManager.java
- 禁用自动启动（默认改为 false）
- 支持端口动态切换
- 检测端口变化时自动重启服务器

### 2. MultiplayerLobbyActivity.java
- 添加服务器启动检查
- 添加端口匹配检查
- 改进轮询机制，防止卡死
- 详细的错误提示和诊断建议

### 3. 用户体验改进
- 端口验证（1-65535）
- 端口不匹配时提供选择
- 显示完整服务器地址（IP:端口）
- Activity 状态检查，防止内存泄漏

## 技术细节

### 端口管理
```java
// 检测端口变化，自动重启
if (isServerRunning && server != null && server.getPort() != port) {
    stopServer();
    Thread.sleep(500);
}
```

### 服务器检查
```java
// 创建房间前检查服务器
if (!serverManager.isServerRunning()) {
    showError("请先启动服务器");
    return;
}
```

### 轮询优化
```java
// 检查 Activity 状态
if (currentRoomId == null || !isRoomCreator || isFinishing()) {
    return; // 停止轮询
}
```

## 故障排查

1. **查看服务器状态**
   - 界面上方显示"✅ 本地服务器运行中"表示正常
   - 显示"⚠️ 本地服务器未运行"表示需要启动

2. **查看日志**
   - 使用 Android Studio 的 Logcat
   - 过滤标签：MultiplayerLobby, ServerManager, BattleClient

3. **重置步骤**
   - 关闭所有应用
   - 重新启动
   - 按照正确流程操作

## 联系支持

如果问题仍然存在，请提供：
- 错误提示截图
- Logcat 日志
- 操作步骤描述
