# 独立服务器使用说明

## 🎯 解决方案

现在您不需要某个设备当主机了！使用独立服务器，所有设备都是客户端。

## 📋 使用步骤

### 方式一：在电脑上运行服务器（推荐）

#### 1. 启动服务器
双击运行：`启动独立服务器.bat`

或者在命令行中运行：
```bash
gradlew :server:run
```

#### 2. 查看服务器信息
服务器启动后会显示：
```
═══════════════════════════════════════════════
  飞机大战联机对战服务器已启动
  端口: 8080
  时间: ...
═══════════════════════════════════════════════

客户端连接地址: http://localhost:8080
模拟器使用: http://10.0.2.2:8080

按 Ctrl+C 停止服务器
```

#### 3. 配置客户端
在所有Android设备上：
- 打开应用，进入"多人对战大厅"
- 服务器地址：`10.0.2.2`（模拟器）或电脑IP地址（真机）
- 端口号：`8080`
- **不要点击"使用本地"按钮**
- 直接点击"创建房间"或"加入房间"

#### 4. 开始游戏
- 设备A：点击"创建房间"，记下房间ID
- 设备B：输入房间ID，点击"加入房间"
- 双方点击"开始对战"

---

### 方式二：在一台设备上运行服务器（原方式）

如果您还是想在某个Android设备上运行服务器：

#### 1. 在设备A上
- 打开应用，进入"多人对战大厅"
- 点击"📱 使用本地"按钮
- 点击"启动"
- 等待"✅ 服务器已启动"
- 点击"创建房间"

#### 2. 在设备B上
- 服务器地址：`10.0.2.2`
- 端口号：`8080`
- 输入房间ID
- 点击"加入房间"

---

## 🔧 获取电脑IP地址

### Windows:
```bash
ipconfig
```
查找"IPv4 地址"，例如：`192.168.1.100`

### Mac/Linux:
```bash
ifconfig
```
或
```bash
ip addr
```

---

## 📱 真机连接配置

如果使用真机（非模拟器）：

1. **确保电脑和手机在同一WiFi网络**
2. **获取电脑IP地址**（如上）
3. **在手机上设置服务器地址为电脑IP**
   - 例如：`192.168.1.100`
   - 端口：`8080`

---

## ⚠️ 常见问题

### 问题1：无法连接到服务器
**解决方案：**
- 检查服务器是否正在运行
- 检查防火墙是否允许端口8080
- 确认IP地址和端口号正确

### 问题2：模拟器连接失败
**解决方案：**
- 模拟器必须使用 `10.0.2.2`
- 不能使用 `localhost` 或 `127.0.0.1`
- 不能使用 `10.0.2.15` 或 `10.0.2.16`

### 问题3：真机连接失败
**解决方案：**
- 确保手机和电脑在同一WiFi
- 使用电脑的实际IP地址（如 `192.168.1.100`）
- 检查电脑防火墙设置

### 问题4：创建房间失败
**解决方案：**
- 如果使用独立服务器，**不要**点击"使用本地"
- 直接输入服务器地址和端口
- 然后点击"创建房间"

---

## 🎮 完整流程示例

### 使用独立服务器（推荐）

```
电脑：
1. 双击 "启动独立服务器.bat"
2. 看到 "服务器已启动" 提示
3. 记下IP地址（如 192.168.1.100）

模拟器A：
1. 服务器地址：10.0.2.2
2. 端口：8080
3. 点击"创建房间"
4. 记下房间ID

模拟器B：
1. 服务器地址：10.0.2.2
2. 端口：8080
3. 输入房间ID
4. 点击"加入房间"

双方：
点击"开始对战"
```

---

## 🛠️ 高级配置

### 修改服务器端口

编辑 `server/src/main/java/edu/hitsz/server/BattleServer.java`：
```java
private static final int PORT = 8080;  // 改为其他端口
```

### 查看服务器日志

服务器会实时显示：
- `[创建房间]` - 房间创建记录
- `[加入房间]` - 玩家加入记录
- `[对战结束]` - 对战结果
- `[房间关闭]` - 房间关闭记录

---

## 📊 对比

| 方式 | 优点 | 缺点 |
|------|------|------|
| **独立服务器** | ✅ 所有设备都是客户端<br>✅ 服务器稳定<br>✅ 易于调试 | ⚠️ 需要额外的电脑 |
| **设备内置服务器** | ✅ 不需要额外设备<br>✅ 便携 | ⚠️ 某个设备当主机<br>⚠️ 主机退出则游戏结束 |

---

## 💡 建议

1. **开发测试时**：使用独立服务器，方便调试
2. **正式游戏时**：可以使用设备内置服务器，更便携
3. **多人联机时**：建议使用独立服务器，更稳定

---

## 🔍 故障排查

### 检查服务器是否运行
```bash
# Windows
netstat -ano | findstr :8080

# Mac/Linux
lsof -i :8080
```

### 测试服务器连接
在浏览器中访问：
```
http://localhost:8080/battle/status?roomId=test&playerId=test
```

应该返回：
```json
{"error":"房间不存在"}
```
这说明服务器正常运行。

---

## 📞 技术支持

如果还有问题，请提供：
1. 服务器启动日志
2. 客户端错误提示
3. 网络配置信息（IP地址、端口号）
