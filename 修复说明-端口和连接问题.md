# 修复说明 - 端口配置和连接问题

## 修复日期
2026年5月11日

## 问题描述
1. **端口号硬编码**：BattleClient 中端口固定为 8080，用户无法自定义端口
2. **localhost 连接问题**：两个设备都使用 localhost，但 localhost 指向各自设备，导致无法互联
3. **房间加入失败**：加入者提示"房间不存在"
4. **轮询卡死**：错误处理不当导致持续轮询，界面卡死

## 修复内容

### 1. BattleClient.java
**修改位置**：`app/src/main/java/edu/hitsz/network/BattleClient.java`

**修改内容**：
- 添加 `port` 字段存储端口号
- 修改构造函数，接收 `serverIp` 和 `port` 两个参数
- 动态构建 baseUrl，使用用户指定的端口

```java
// 修改前
public BattleClient(String serverIp) {
    this.baseUrl = "http://" + serverIp + ":8080";
    ...
}

// 修改后
public BattleClient(String serverIp, int port) {
    this.port = port;
    this.baseUrl = "http://" + serverIp + ":" + port;
    ...
}
```

### 2. MultiplayerLobbyActivity.java
**修改位置**：`app/src/main/java/edu/hitsz/ui/MultiplayerLobbyActivity.java`

**修改内容**：

#### 2.1 添加端口输入框
- 新增 `etPort` 输入框，允许用户输入端口号
- 默认值为 8080
- 输入类型限制为数字

#### 2.2 显示本机IP地址
- 新增 `tvLocalIp` 显示本机局域网IP
- 添加 `getLocalIpAddress()` 方法获取本机IPv4地址
- 方便用户在另一台设备上输入正确的服务器地址

#### 2.3 端口验证
在 `createRoom()` 和 `joinRoom()` 方法中添加端口验证：
```java
int port = 8080;
if (!portStr.isEmpty()) {
    try {
        port = Integer.parseInt(portStr);
        if (port < 1 || port > 65535) {
            showError("端口号必须在 1-65535 之间");
            return;
        }
    } catch (NumberFormatException e) {
        showError("端口号格式错误");
        return;
    }
}
```

#### 2.4 优化轮询逻辑，避免卡死
- 添加 `pollFailCount` 失败计数器
- 设置 `MAX_POLL_FAILS = 5` 最大失败次数
- 轮询成功时重置计数器
- 超过最大失败次数时停止轮询并提示用户
- 添加详细的日志输出

```java
@Override
public void onError(String error) {
    pollFailCount++;
    Log.w("MultiplayerLobby", "轮询失败 (" + pollFailCount + "/" + MAX_POLL_FAILS + "): " + error);
    
    if (pollFailCount < MAX_POLL_FAILS) {
        pollHandler.postDelayed(self, 2000);
    } else {
        runOnUiThread(() -> {
            showError("无法连接到服务器，请检查服务器地址和端口");
            btnCreateRoom.setEnabled(true);
            btnJoinRoom.setEnabled(true);
        });
    }
}
```

#### 2.5 更新服务器状态显示
- 显示本地服务器运行的端口号
- 启动本地服务器时使用用户设置的端口

#### 2.6 传递端口到游戏界面
在 `startBattle()` 方法中添加端口参数：
```java
intent.putExtra("port", port);
```

### 3. MultiplayerGameActivity.java
**修改位置**：`app/src/main/java/edu/hitsz/ui/MultiplayerGameActivity.java`

**修改内容**：
- 添加 `port` 字段
- 从 Intent 中获取端口参数：`port = getIntent().getIntExtra("port", 8080);`
- 创建 BattleClient 时传入端口：`battleClient = new BattleClient(serverIp, port);`

## 使用说明

### 本地测试（单设备）
1. 点击"📱 使用本地"按钮启动本地服务器
2. 服务器地址保持 `localhost`
3. 端口号默认 `8080`（可自定义）
4. 创建房间或加入房间

### 局域网联机（多设备）
1. **设备A（创建房间）**：
   - 查看界面顶部显示的"本机IP"（例如：192.168.1.100）
   - 点击"📱 使用本地"启动服务器
   - 服务器地址设为 `localhost`
   - 端口号设为 `8080`（或自定义）
   - 点击"🎮 创建房间"
   - 记下房间ID

2. **设备B（加入房间）**：
   - 服务器地址输入设备A的IP（例如：192.168.1.100）
   - 端口号输入与设备A相同的端口（例如：8080）
   - 输入设备A显示的房间ID
   - 点击"🚪 加入房间"

### 自定义端口
如果默认端口 8080 被占用，可以修改端口号：
1. 在端口输入框中输入其他端口（例如：8888）
2. 确保创建者和加入者使用相同的端口号
3. 端口范围：1-65535

## 注意事项

1. **网络连接**：确保两台设备在同一局域网内
2. **防火墙**：如果连接失败，检查防火墙是否阻止了端口
3. **端口一致**：创建者和加入者必须使用相同的端口号
4. **IP地址**：加入者必须输入创建者的实际IP地址，不能使用 localhost
5. **轮询限制**：如果连续5次轮询失败，系统会自动停止并提示错误

## 技术细节

### 端口配置流程
```
用户输入端口 → 验证端口范围 → 创建BattleClient → 连接服务器
```

### 轮询机制
```
开始轮询 → 检查房间状态 → 成功：重置计数 / 失败：计数+1
         ↓
    计数 < 5 → 继续轮询（2秒后）
    计数 ≥ 5 → 停止轮询，提示错误
```

### IP地址获取
遍历所有网络接口，查找非回环、已启用的IPv4地址：
```java
NetworkInterface.getNetworkInterfaces() 
→ 过滤回环和未启用接口 
→ 获取IPv4地址 
→ 返回第一个有效地址
```

## 测试建议

1. **单设备测试**：使用 localhost 测试基本功能
2. **双设备测试**：使用两台真机或模拟器测试局域网连接
3. **端口冲突测试**：测试端口被占用时的错误提示
4. **网络异常测试**：测试断网、超时等异常情况
5. **轮询限制测试**：测试服务器不可达时的自动停止机制

## 相关文件

- `app/src/main/java/edu/hitsz/network/BattleClient.java`
- `app/src/main/java/edu/hitsz/ui/MultiplayerLobbyActivity.java`
- `app/src/main/java/edu/hitsz/ui/MultiplayerGameActivity.java`
- `app/src/main/java/edu/hitsz/server/EmbeddedBattleServer.java`
- `app/src/main/java/edu/hitsz/server/ServerManager.java`
