# 联机问题完整排查指南

## 问题现象
创建房间和加入房间都失败

## 完整排查步骤

### 第1步：确认只有一个设备启动服务器

**错误做法 ❌**
```
模拟器A：启动服务器 → 创建房间
模拟器B：启动服务器 → 加入房间  ← 错误！
```

**正确做法 ✅**
```
模拟器A：启动服务器 → 创建房间
模拟器B：不启动服务器 → 加入房间
```

### 第2步：确认服务器地址配置

**模拟器场景（同一台电脑）**
```
模拟器A（创建者）：
- 点击"使用本地"启动服务器
- 服务器地址：10.0.2.2
- 端口：8080
- 创建房间

模拟器B（加入者）：
- 不启动服务器
- 服务器地址：10.0.2.2
- 端口：8080
- 输入房间ID
- 加入房间
```

**真机场景（局域网）**
```
手机A（创建者）：
- 启动服务器
- 查看本机IP（例如：192.168.1.100）
- 创建房间

手机B（加入者）：
- 不启动服务器
- 点击"扫描局域网服务器"
- 选择手机A的服务器
- 或手动输入：192.168.1.100:8080
- 加入房间
```

### 第3步：检查日志

打开Android Studio的Logcat，筛选标签：
- `BattleClient` - 客户端请求日志
- `EmbeddedServer` - 服务器日志
- `MultiplayerLobby` - 大厅逻辑日志

**正常日志示例：**
```
BattleClient: 初始化BattleClient: http://10.0.2.2:8080
BattleClient: 创建房间请求: http://10.0.2.2:8080/battle/create
EmbeddedServer: [创建房间] ROOM_1778488262573 by Player1
BattleClient: 创建房间响应码: 200
```

**异常日志示例：**
```
BattleClient: 创建房间网络失败: java.net.ConnectException: Failed to connect
```

### 第4步：网络连接测试

**测试服务器是否启动：**
1. 在启动服务器的设备上，查看日志：
   ```
   EmbeddedServer: 服务器已启动，端口: 8080
   ```

2. 如果没有看到此日志，服务器未启动

**测试网络连通性：**
- 模拟器：10.0.2.2 是特殊地址，指向宿主机
- 真机：确保在同一WiFi网络

### 第5步：常见错误及解决

#### 错误1：网络错误 - Connection refused
**原因**：服务器未启动或地址错误
**解决**：
1. 确认创建者已启动服务器
2. 检查服务器地址是否正确
3. 模拟器必须使用 10.0.2.2

#### 错误2：房间不存在
**原因**：
1. 两个设备连接到不同的服务器
2. 房间ID输入错误

**解决**：
1. 确认只有一个设备启动服务器
2. 仔细核对房间ID

#### 错误3：服务器错误 405
**原因**：服务器未正确启动或端口被占用
**解决**：
1. 重启应用
2. 更换端口（如8888）
3. 检查是否有其他应用占用端口

#### 错误4：轮询失败次数过多
**原因**：网络不稳定或服务器无响应
**解决**：
1. 检查网络连接
2. 重新启动服务器
3. 确认防火墙未阻止

### 第6步：使用扫描功能

**推荐方法（最简单）：**
1. 模拟器A：启动服务器 → 创建房间
2. 模拟器B：点击"🔍 扫描局域网服务器"
3. 等待3秒，查看发现的服务器
4. 点击选择服务器（自动填充IP和端口）
5. 输入房间ID → 加入房间

**如果扫描不到服务器：**
1. 确认创建者已启动服务器
2. 确认在同一网络（模拟器都用10.0.2.2）
3. 检查防火墙设置
4. 尝试手动输入IP和端口

### 第7步：端口配置

**默认端口：8080**

**如果8080被占用：**
1. 两个设备都修改为相同端口（如8888）
2. 创建者：启动服务器时使用新端口
3. 加入者：输入相同端口

**端口范围：1-65535**

### 第8步：权限检查

确认AndroidManifest.xml包含以下权限：
```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
<uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
```

### 第9步：网络安全配置

确认允许明文HTTP通信（已配置）：
```xml
<base-config cleartextTrafficPermitted="true">
```

### 第10步：完整测试流程

**模拟器测试（推荐）：**

1. **启动模拟器A**
   - 打开应用
   - 进入多人对战大厅
   - 点击"使用本地"
   - 确认提示"服务器已启动"
   - 服务器地址：10.0.2.2
   - 端口：8080
   - 点击"创建房间"
   - 记下房间ID

2. **启动模拟器B**
   - 打开应用
   - 进入多人对战大厅
   - 不要点击"使用本地"
   - 服务器地址：10.0.2.2
   - 端口：8080
   - 输入房间ID
   - 点击"加入房间"

3. **开始对战**
   - 模拟器A：等待提示"对手已加入"
   - 两边都点击"开始对战"

## 调试技巧

### 查看详细日志
```
adb logcat | findstr "BattleClient EmbeddedServer MultiplayerLobby"
```

### 测试服务器连接
在浏览器访问：
```
http://10.0.2.2:8080/battle/status?roomId=test&playerId=test
```

应该返回JSON响应（即使房间不存在）

### 检查端口占用
```
netstat -ano | findstr :8080
```

## 成功标志

**创建房间成功：**
- 显示房间ID
- 状态："等待对手加入..."
- 日志：`[创建房间] ROOM_xxx by PlayerName`

**加入房间成功：**
- 显示"已加入房间"
- 显示对手名称
- "开始对战"按钮可点击

**服务器运行正常：**
- 状态显示："✅ 本地服务器运行中"
- 日志：`服务器已启动，端口: 8080`
- 日志：`服务器广播已启动，端口: 8888`

## 仍然无法解决？

1. 重启两个设备的应用
2. 清除应用数据
3. 重新安装APK
4. 检查Android版本（需要API 30+）
5. 尝试使用真机测试
6. 查看完整日志文件

## 联系支持

提供以下信息：
1. 设备类型（模拟器/真机）
2. Android版本
3. 完整错误日志
4. 操作步骤截图
5. 网络配置信息
