# 模拟器联机正确步骤

## ⚠️ 核心问题
**两个模拟器都启动了各自的服务器，导致它们连接的是不同的服务器，所以找不到对方的房间！**

## ✅ 正确做法

### 关键原则
**只有一个设备（创建房间的）启动服务器，另一个设备（加入房间的）连接到它！**

---

## 📱 模拟器A（创建房间）

### 步骤：
1. **启动服务器**
   - 点击"📱 使用本地"按钮
   - 确认启动本地服务器
   - 等待提示"✅ 服务器已启动，现在可以创建房间"

2. **配置连接**
   - 服务器地址：`localhost`（自动填充）
   - 端口号：`8080`（默认）

3. **创建房间**
   - 点击"🎮 创建房间"
   - 记下房间ID（例如：ROOM_1778488262573）
   - 查看顶部显示的"本机IP"（例如：10.0.2.15）

4. **等待对手**
   - 界面会显示"等待对手加入..."
   - 告诉对方你的IP地址和房间ID

---

## 📱 模拟器B（加入房间）

### 步骤：
1. **不要启动服务器！**
   - ❌ 不要点击"使用本地"按钮
   - ❌ 不要启动本地服务器
   - 如果已经启动，请重启应用

2. **配置连接**
   - 服务器地址：输入模拟器A的IP（例如：`10.0.2.2`）
   - 端口号：`8080`（与模拟器A相同）
   - 房间ID：输入模拟器A显示的房间ID

3. **加入房间**
   - 点击"🚪 加入房间"
   - 成功后会显示"已加入房间"

4. **开始对战**
   - 双方都点击"⚔️ 开始对战"

---

## 🔧 模拟器特殊配置

### Android模拟器网络地址
- `10.0.2.2` = 宿主机的 localhost
- `10.0.2.15` = 模拟器自己的IP

### 如果两个模拟器在同一台电脑上：
**模拟器A（创建房间）：**
- 服务器地址：`localhost`
- 启动本地服务器

**模拟器B（加入房间）：**
- 服务器地址：`10.0.2.2`（指向宿主机）
- 不启动服务器

---

## ❌ 常见错误

### 错误1：两个设备都启动服务器
```
模拟器A：启动服务器 → 创建房间 → 房间在服务器A
模拟器B：启动服务器 → 加入房间 → 连接到服务器B → 找不到房间！
```
**原因**：两个独立的服务器，房间数据不共享

### 错误2：加入者使用 localhost
```
模拟器B：服务器地址 = localhost → 连接到自己的设备 → 找不到房间！
```
**原因**：localhost 只能连接到本设备

### 错误3：端口号不一致
```
模拟器A：端口 8080
模拟器B：端口 8888 → 连接失败！
```
**原因**：端口号必须相同

---

## 🎯 快速检查清单

### 创建房间前：
- [ ] 只有创建者启动了服务器
- [ ] 服务器地址是 `localhost`
- [ ] 端口号已设置（默认8080）
- [ ] 记下本机IP地址

### 加入房间前：
- [ ] 没有启动本地服务器
- [ ] 服务器地址是创建者的IP（不是localhost）
- [ ] 端口号与创建者相同
- [ ] 已获得房间ID

---

## 🆘 故障排查

### 问题：提示"房间不存在"
**可能原因：**
1. 加入者也启动了服务器 → 关闭服务器，重启应用
2. 服务器地址错误 → 检查是否使用了创建者的IP
3. 端口号不一致 → 确认两边端口相同

### 问题：连接超时
**可能原因：**
1. 网络不通 → 检查模拟器网络配置
2. 防火墙阻止 → 关闭防火墙或添加例外
3. IP地址错误 → 确认使用正确的IP

### 问题：界面卡死
**已修复**：现在会在5次失败后自动停止轮询

---

## 💡 提示信息说明

### 界面提示
- **"⚠️ 联机提示：只有创建房间的设备需要启动服务器！"**
  - 提醒用户不要两边都启动服务器

- **"创建房间用localhost，加入房间用对方IP"**
  - 输入框提示，说明正确的配置方式

### 警告对话框
- **创建房间时使用非localhost地址**
  - 系统会警告并确认

- **加入房间时使用localhost**
  - 系统会阻止并提示正确做法

---

## 📝 总结

**一句话记住：**
> 创建者启动服务器用localhost，加入者不启动服务器用创建者IP！

**图示：**
```
模拟器A（创建者）          模拟器B（加入者）
    ↓                          ↓
启动服务器                  不启动服务器
    ↓                          ↓
localhost:8080            10.0.2.2:8080
    ↓                          ↓
创建房间                    加入房间
    ↓                          ↓
        ← 同一个服务器 →
```
