# 飞机大战 - 多人联机测试指南

## 📋 目录
1. [功能概述](#功能概述)
2. [环境准备](#环境准备)
3. [服务器启动](#服务器启动)
4. [客户端配置](#客户端配置)
5. [测试步骤](#测试步骤)
6. [常见问题](#常见问题)
7. [音效问题解决](#音效问题解决)

---

## 🎮 功能概述

### 已实现功能
✅ 多人对战大厅（创建房间/加入房间）  
✅ 实时分数同步（每秒更新）  
✅ 对战状态显示（对手分数、死亡状态）  
✅ 双方死亡后自动结算  
✅ 胜负判定和结果展示  
✅ HTTP服务器（端口8080）  
✅ 支持多开模拟器同屏对战  

### 技术架构
- **服务器**: Java HttpServer (端口8080)
- **客户端**: OkHttp + Gson
- **通信协议**: HTTP REST API
- **同步机制**: 轮询（1秒间隔）

---

## 🔧 环境准备

### 1. 必需软件
- ✅ Android Studio (已安装)
- ✅ JDK 11+ (已配置)
- ✅ Android SDK (API 30+)

### 2. 模拟器配置
需要创建**至少2个**Android模拟器用于测试：

#### 方法1：使用Android Studio AVD Manager
1. 打开 Android Studio
2. 点击 Tools > Device Manager
3. 点击 "Create Device"
4. 选择设备型号（推荐 Pixel 5）
5. 选择系统镜像（API 30+）
6. 完成创建
7. **重复以上步骤创建第二个模拟器**

#### 方法2：使用命令行
```bash
# 列出可用的系统镜像
sdkmanager --list

# 创建模拟器1
avdmanager create avd -n Player1 -k "system-images;android-30;google_apis;x86_64"

# 创建模拟器2
avdmanager create avd -n Player2 -k "system-images;android-30;google_apis;x86_64"
```

---

## 🚀 服务器启动

### 方法1：使用启动脚本（推荐）
1. 双击项目根目录的 `启动服务器.bat`
2. 等待服务器启动完成
3. 看到以下信息表示成功：
```
═══════════════════════════════════════════════
  飞机大战联机对战服务器已启动
  端口: 8080
  时间: Sun May 11 11:00:00 CST 2026
═══════════════════════════════════════════════

客户端连接地址: http://localhost:8080
模拟器使用: http://10.0.2.2:8080
```

### 方法2：使用Gradle命令
```bash
# Windows
gradlew.bat :server:run

# Linux/Mac
./gradlew :server:run
```

### 方法3：在Android Studio中运行
1. 打开 `server/src/main/java/edu/hitsz/server/BattleServer.java`
2. 右键点击 `main` 方法
3. 选择 "Run 'BattleServer.main()'"

### 验证服务器运行
服务器启动后，可以通过浏览器访问测试：
- 打开浏览器
- 访问 `http://localhost:8080/battle/status?roomId=test&playerId=test`
- 应该返回 JSON 错误信息（房间不存在），说明服务器正常运行

---

## 📱 客户端配置

### 模拟器网络配置

#### 重要：模拟器访问本机服务器的特殊IP
Android模拟器有特殊的网络配置：
- ❌ **不能使用** `localhost` 或 `127.0.0.1`（这会指向模拟器自己）
- ✅ **必须使用** `10.0.2.2`（这是模拟器访问宿主机的特殊IP）

#### 服务器地址配置
在游戏的多人对战大厅中：
- **模拟器测试**: 输入 `10.0.2.2`
- **真机测试**: 输入电脑的局域网IP（如 `192.168.1.100`）

#### 查看电脑局域网IP
```bash
# Windows
ipconfig

# Linux/Mac
ifconfig
```
找到 `192.168.x.x` 或 `10.0.x.x` 格式的IP地址

---

## 🧪 测试步骤

### 完整测试流程

#### 步骤1：启动服务器
1. 双击 `启动服务器.bat`
2. 确认服务器启动成功（看到启动信息）
3. **保持服务器窗口运行，不要关闭**

#### 步骤2：启动第一个模拟器（玩家1）
1. 在Android Studio中打开 Device Manager
2. 启动第一个模拟器（如 Pixel_5_API_30）
3. 等待模拟器完全启动

#### 步骤3：安装并运行游戏（玩家1）
```bash
# 方法1：使用Android Studio
点击 Run 按钮，选择第一个模拟器

# 方法2：使用命令行
gradlew.bat installDebug
adb -s emulator-5554 shell am start -n edu.hitsz/.ui.LoginActivity
```

#### 步骤4：玩家1创建房间
1. 在游戏中登录/注册账号（如 "Player1"）
2. 进入主菜单
3. 点击 **"⚔️ 多人对战"** 按钮
4. 在服务器地址输入框输入：`10.0.2.2`
5. 点击 **"🎮 创建房间"** 按钮
6. 记录显示的房间ID（如 `ROOM_1715400000000`）
7. 等待对手加入（界面会显示"等待对手加入..."）

#### 步骤5：启动第二个模拟器（玩家2）
1. 在Device Manager中启动第二个模拟器
2. 等待完全启动
3. 安装游戏APK到第二个模拟器：
```bash
# 查看连接的设备
adb devices

# 安装到第二个模拟器
adb -s emulator-5556 install app/build/outputs/apk/debug/app-debug.apk
```

#### 步骤6：玩家2加入房间
1. 在第二个模拟器中启动游戏
2. 登录/注册账号（如 "Player2"）
3. 进入主菜单，点击 **"⚔️ 多人对战"**
4. 在服务器地址输入框输入：`10.0.2.2`
5. 在房间ID输入框输入玩家1的房间ID
6. 点击 **"🚪 加入房间"** 按钮
7. 看到"已加入房间"提示

#### 步骤7：开始对战
1. 两个玩家都点击 **"⚔️ 开始对战"** 按钮
2. 游戏开始，可以看到：
   - 左上角显示"我: 0"（自己的分数，绿色）
   - 右上角显示"对手: 0"（对手的分数，红色）
3. 开始游戏，击杀敌机获得分数
4. 分数会实时同步到对手屏幕

#### 步骤8：观察同步效果
- ✅ 玩家1击杀敌机 → 玩家2屏幕上看到"对手"分数增加
- ✅ 玩家2击杀敌机 → 玩家1屏幕上看到"对手"分数增加
- ✅ 任一玩家死亡 → 对方屏幕显示"对手已死亡"
- ✅ 双方都死亡 → 自动跳转到结算页面

#### 步骤9：查看结算结果
对战结束后，两个玩家都会看到：
- 🎉 胜利！（分数高的一方）
- 💔 失败（分数低的一方）
- 🤝 平局（分数相同）
- 显示双方最终分数和分数差距

---

## 🐛 常见问题

### 问题1：无法连接服务器
**症状**: 点击"创建房间"或"加入房间"后提示"网络错误"

**解决方案**:
1. 确认服务器正在运行（检查服务器窗口）
2. 确认使用正确的IP地址：
   - 模拟器必须使用 `10.0.2.2`
   - 不要使用 `localhost` 或 `127.0.0.1`
3. 检查防火墙是否阻止了8080端口
4. 尝试重启服务器

### 问题2：房间不存在
**症状**: 加入房间时提示"房间不存在"

**解决方案**:
1. 确认房间ID输入正确（区分大小写）
2. 确认玩家1已经成功创建房间
3. 检查服务器日志，确认房间创建成功

### 问题3：分数不同步
**症状**: 一方分数变化，另一方看不到

**解决方案**:
1. 检查网络连接
2. 查看服务器日志，确认收到更新请求
3. 确认两个客户端都连接到同一个服务器
4. 尝试重新开始对战

### 问题4：对战无法结束
**症状**: 双方都死亡，但没有跳转到结算页面

**解决方案**:
1. 等待1-2秒（有延迟）
2. 检查服务器日志，确认收到死亡状态
3. 手动返回主菜单，重新开始

### 问题5：模拟器卡顿
**症状**: 游戏运行缓慢，帧率低

**解决方案**:
1. 关闭不必要的应用程序
2. 在AVD Manager中增加模拟器的RAM和CPU核心数
3. 启用硬件加速（HAXM或Hyper-V）
4. 使用性能更好的模拟器配置

### 问题6：无法同时运行两个模拟器
**症状**: 启动第二个模拟器时第一个自动关闭

**解决方案**:
1. 确保电脑有足够的内存（建议16GB+）
2. 减少每个模拟器的RAM分配（如改为2GB）
3. 使用轻量级的系统镜像（不带Google Play）

---

## 🔊 音效问题解决

### 当前状态
❌ 音频文件缺失 - 游戏可以运行，但没有音效

### 解决方案

#### 方法1：添加音频文件（推荐）
1. 准备以下音频文件：
   - `bgm.mp3` - 背景音乐
   - `sound_shoot.wav` - 射击音效
   - `sound_enemy_die.wav` - 敌机死亡
   - `sound_hero_die.wav` - 英雄死亡
   - `sound_prop.wav` - 拾取道具
   - `sound_bomb.wav` - 炸弹爆炸

2. 将文件复制到：
   ```
   app/src/main/res/raw/
   ```

3. 重新编译项目

#### 方法2：使用免费音效资源
推荐网站：
- [Freesound.org](https://freesound.org/) - 免费音效库
- [OpenGameArt.org](https://opengameart.org/) - 开源游戏资源
- [Zapsplat.com](https://www.zapsplat.com/) - 免费音效下载

#### 方法3：临时禁用音效
如果暂时不需要音效，游戏会自动处理文件缺失，不会崩溃。

详细说明请查看：`app/src/main/res/raw/音频文件说明.txt`

---

## 📊 测试检查清单

### 服务器测试
- [ ] 服务器成功启动
- [ ] 端口8080可访问
- [ ] 服务器日志正常输出

### 客户端测试
- [ ] 两个模拟器都成功启动
- [ ] 游戏APK成功安装到两个模拟器
- [ ] 两个玩家都能登录游戏

### 联机功能测试
- [ ] 玩家1成功创建房间
- [ ] 玩家2成功加入房间
- [ ] 双方都能开始对战
- [ ] 分数实时同步（1秒内）
- [ ] 对手死亡状态正确显示
- [ ] 双方死亡后自动结算
- [ ] 结算页面正确显示胜负

### 边界情况测试
- [ ] 重复创建房间
- [ ] 加入不存在的房间
- [ ] 房间已满时加入
- [ ] 网络断开后重连
- [ ] 中途退出游戏

---

## 🎯 性能指标

### 预期性能
- **分数同步延迟**: < 1秒
- **房间创建时间**: < 500ms
- **加入房间时间**: < 500ms
- **游戏帧率**: 60 FPS
- **网络带宽**: < 1 KB/s per player

### 监控方法
1. 查看服务器日志的时间戳
2. 使用Android Studio的Profiler监控性能
3. 观察游戏画面流畅度

---

## 📝 测试报告模板

```
测试日期: 2026/5/11
测试人员: [姓名]
测试环境: 
  - 操作系统: Windows 11
  - Android Studio版本: [版本号]
  - 模拟器配置: [配置信息]

测试结果:
✅ 服务器启动: 成功/失败
✅ 创建房间: 成功/失败
✅ 加入房间: 成功/失败
✅ 分数同步: 成功/失败
✅ 对战结算: 成功/失败

发现的问题:
1. [问题描述]
2. [问题描述]

建议:
1. [改进建议]
2. [改进建议]
```

---

## 🆘 获取帮助

### 查看日志
1. **服务器日志**: 查看服务器窗口的输出
2. **Android日志**: 
   ```bash
   adb logcat | grep "BattleClient"
   ```

### 联系支持
如果遇到无法解决的问题，请提供：
1. 详细的错误描述
2. 服务器日志
3. Android logcat日志
4. 测试环境信息

---

## ✅ 测试完成

恭喜！如果你完成了以上所有测试步骤，说明多人联机功能已经正常工作。

祝游戏愉快！🎮
