# 多设备联机正确配置指南

## 问题说明

**错误现象**：加入房间失败：房间不存在

**根本原因**：两个设备都使用"localhost"，但localhost在每个设备上指向自己的本地服务器，不是同一个服务器！

## 正确配置方法

### 场景1：两个模拟器联机（推荐用于测试）

#### 设备A（创建房间）
1. 服务器地址：`localhost`
2. 点击"📱 使用本地"按钮
3. 点击"🎮 创建房间"
4. 记下房间ID（如：ROOM_1778488262573）

#### 设备B（加入房间）
1. 服务器地址：`10.0.2.2`（这是模拟器访问主机的特殊地址）
2. 输入房间ID
3. 点击"🚪 加入房间"

**原理**：
- 设备A在自己的localhost上启动服务器
- 设备B通过10.0.2.2访问主机（电脑），实际连接到设备A的服务器

---

### 场景2：真机+模拟器联机

#### 方案A：真机创建房间（推荐）

**真机（创建房间）**
1. 确保真机和电脑在同一WiFi
2. 服务器地址：`localhost`
3. 点击"📱 使用本地"
4. 创建房间

**模拟器（加入房间）**
1. 服务器地址：`10.0.2.2`
2. 输入房间ID
3. 加入房间

#### 方案B：模拟器创建房间

**模拟器（创建房间）**
1. 服务器地址：`localhost`
2. 创建房间

**真机（加入房间）**
1. 先查看电脑的局域网IP：
   - Windows：打开CMD，输入`ipconfig`，找到"无线局域网适配器 WLAN"的IPv4地址
   - 例如：`192.168.1.100`
2. 服务器地址：填入电脑的IP（如`192.168.1.100`）
3. 输入房间ID
4. 加入房间

---

### 场景3：两个真机联机

#### 真机A（创建房间）
1. 确保两个真机在同一WiFi
2. 服务器地址：`localhost`
3. 点击"📱 使用本地"
4. 创建房间
5. **查看真机A的IP地址**：
   - 设置 → WLAN → 点击已连接的WiFi → 查看IP地址
   - 例如：`192.168.1.105`

#### 真机B（加入房间）
1. 服务器地址：填入真机A的IP（如`192.168.1.105`）
2. 输入房间ID
3. 加入房间

---

## 快速诊断

### 问题：加入房间失败：房间不存在

**检查清单**：
- [ ] 创建房间的设备是否使用了"localhost"或"📱 使用本地"？
- [ ] 加入房间的设备是否使用了正确的IP地址？
- [ ] 两个设备是否在同一网络？（真机情况）
- [ ] 房间ID是否复制正确？

### 问题：网络错误

**可能原因**：
1. 服务器未启动
2. IP地址错误
3. 端口被占用
4. 防火墙阻止

**解决方法**：
1. 检查创建房间设备的服务器状态（应显示"✅ 本地服务器运行中"）
2. 确认IP地址正确
3. 尝试重启应用
4. 关闭防火墙（测试用）

---

## 配置示例

### 示例1：两个模拟器（最简单）

```
模拟器1（创建）：
- 服务器地址：localhost
- 操作：创建房间
- 房间ID：ROOM_1778488262573

模拟器2（加入）：
- 服务器地址：10.0.2.2
- 房间ID：ROOM_1778488262573
- 操作：加入房间
```

### 示例2：两个真机

```
真机A（创建）：
- IP地址：192.168.1.105
- 服务器地址：localhost
- 操作：创建房间
- 房间ID：ROOM_1778488262573

真机B（加入）：
- 服务器地址：192.168.1.105  ← 使用真机A的IP
- 房间ID：ROOM_1778488262573
- 操作：加入房间
```

---

## 常见错误

### ❌ 错误配置1：两个设备都用localhost

```
设备A：localhost → 创建房间
设备B：localhost → 加入房间 ❌ 失败！
```

**原因**：设备B连接到自己的服务器，找不到设备A创建的房间

### ❌ 错误配置2：创建房间的设备用了外部IP

```
设备A：192.168.1.100 → 创建房间 ❌ 失败！
设备B：192.168.1.100 → 加入房间
```

**原因**：设备A应该连接自己的localhost，不应该连接外部IP

### ✅ 正确配置

```
设备A：localhost → 创建房间 ✅
设备B：192.168.1.100（设备A的IP）→ 加入房间 ✅
```

---

## 如何查看IP地址

### Windows电脑
```bash
ipconfig
```
查找"无线局域网适配器 WLAN"的IPv4地址

### Android真机
1. 设置 → WLAN
2. 点击已连接的WiFi名称
3. 查看IP地址

### 模拟器
- 模拟器访问主机：使用`10.0.2.2`
- 主机访问模拟器：通常不需要

---

## 测试步骤

### 第一步：确认服务器启动
1. 打开创建房间的设备
2. 进入"多人对战大厅"
3. 确认显示"✅ 本地服务器运行中 (localhost:8080)"

### 第二步：创建房间
1. 服务器地址：`localhost`
2. 点击"📱 使用本地"
3. 点击"🎮 创建房间"
4. 等待显示房间ID

### 第三步：加入房间
1. 在另一个设备上打开应用
2. 服务器地址：填入正确的IP（见上面场景说明）
3. 房间ID：输入第二步显示的房间ID
4. 点击"🚪 加入房间"

### 第四步：开始游戏
1. 创建房间的设备会显示"对手已加入！"
2. 两个设备都点击"⚔️ 开始对战"

---

## 故障排查

### 1. 查看日志
```bash
adb logcat | grep -E "EmbeddedServer|BattleClient"
```

### 2. 检查服务器状态
- 创建房间设备应显示："内置服务器已启动，端口: 8080"

### 3. 测试网络连接
```bash
# 在加入房间的设备上测试
adb shell ping <创建房间设备的IP>
```

### 4. 检查防火墙
- Windows：允许Java通过防火墙
- Android：无需特殊配置

---

## 总结

**核心原则**：
1. **创建房间的设备**：使用`localhost`
2. **加入房间的设备**：使用创建房间设备的IP地址
3. **同一网络**：真机必须在同一WiFi

**记住**：
- localhost = 自己的服务器
- 10.0.2.2 = 模拟器访问主机
- 192.168.x.x = 局域网IP地址

**快速配置**（两个模拟器）：
- 模拟器1：localhost → 创建
- 模拟器2：10.0.2.2 → 加入

现在重新配置后应该可以成功联机了！🎮
