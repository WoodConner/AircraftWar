# 模拟器联机终极方案

## 问题分析

### 问题1：两个模拟器IP相同
- 模拟器获取的IP都是 `10.0.2.15`（模拟器自己的IP）
- 无法通过IP区分不同的模拟器

### 问题2：需要支持多人加入
- 当前代码只支持2人对战（player1和player2）
- 需要扩展为支持多个房间，每个房间2人

## 解决方案

### 方案：使用宿主机作为中心服务器

```
宿主机（电脑）
    ↓
运行服务器（localhost:8080）
    ↓
    ├─→ 模拟器A（10.0.2.2:8080）─→ 创建房间
    └─→ 模拟器B（10.0.2.2:8080）─→ 加入房间
```

## 实施步骤

### 步骤1：在宿主机启动独立服务器

需要创建一个独立的Java服务器程序，运行在宿主机上。

**方法A：使用项目中的server模块**
```bash
cd server
gradle run
```

**方法B：使用内置服务器（推荐）**
在一个模拟器中启动服务器，其他模拟器连接到它。

### 步骤2：配置模拟器

**所有模拟器统一配置：**
- 服务器地址：`10.0.2.2`（指向宿主机）
- 端口号：`8080`

**模拟器A（创建房间）：**
1. 启动应用
2. 点击"使用本地"启动服务器
3. 服务器地址改为：`10.0.2.2`
4. 创建房间

**模拟器B、C、D...（加入房间）：**
1. 启动应用
2. 不启动服务器
3. 服务器地址：`10.0.2.2`
4. 输入房间ID加入

## 当前限制

### 游戏设计限制
当前游戏只支持**1v1对战**：
- 每个房间只能有2个玩家
- 不支持多人混战

### 如果需要多人同时游戏
需要创建多个房间：
- 房间1：玩家A vs 玩家B
- 房间2：玩家C vs 玩家D
- 房间3：玩家E vs 玩家F

## 修改建议

### 当前代码已支持
✅ 自定义端口
✅ 多个房间（通过房间ID区分）
✅ 防止错误配置
✅ 轮询保护

### 需要进一步修改（如果要真正多人）
❌ 游戏逻辑改为支持多人混战
❌ 服务器支持多人房间
❌ UI显示多个玩家状态

## 推荐配置

### 配置1：两个模拟器（最简单）
```
模拟器A：
- 启动服务器
- 服务器地址：10.0.2.2
- 创建房间

模拟器B：
- 不启动服务器
- 服务器地址：10.0.2.2
- 加入房间
```

### 配置2：多个模拟器（多个房间）
```
模拟器A：
- 启动服务器
- 创建房间1

模拟器B：
- 加入房间1

模拟器C：
- 创建房间2

模拟器D：
- 加入房间2
```

## 注意事项

1. **只启动一个服务器**：无论有多少个模拟器，只在一个上启动服务器
2. **统一使用10.0.2.2**：所有模拟器都连接到这个地址
3. **房间ID区分**：通过不同的房间ID实现多组对战
4. **1v1限制**：每个房间只能2人，不能多人混战

## 故障排查

### 问题：模拟器B连接不上
1. 确认模拟器A的服务器已启动
2. 确认两个模拟器都使用 `10.0.2.2`
3. 确认端口号相同
4. 检查防火墙设置

### 问题：想要3人或更多人一起玩
**答**：当前游戏不支持多人混战，只能创建多个房间，每个房间2人对战。

如果需要真正的多人混战，需要大幅修改游戏逻辑。
