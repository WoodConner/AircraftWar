# 服务器故障排查指南

## 问题：创建房间/加入房间失败

### 常见原因和解决方案

#### 1. 服务器未启动
**症状**: 客户端提示"网络错误"或"无法连接到服务器"

**检查方法**:
```bash
# 方法1：使用测试脚本
双击 "测试服务器.bat"

# 方法2：使用浏览器
打开浏览器，访问: http://localhost:8080/battle/status?roomId=test&playerId=test
应该返回JSON错误（房间不存在），说明服务器正常运行

# 方法3：使用命令行
curl http://localhost:8080/battle/status?roomId=test&playerId=test
```

**解决方案**:
1. 双击 `启动服务器.bat` 启动服务器
2. 等待看到"飞机大战联机对战服务器已启动"消息
3. **保持服务器窗口运行，不要关闭**

---

#### 2. 端口8080被占用
**症状**: 服务器启动失败，提示端口已被使用

**检查方法**:
```bash
# Windows
netstat -ano | findstr :8080

# 查看哪个程序占用了端口
tasklist | findstr <PID>
```

**解决方案**:
- **方案A**: 关闭占用8080端口的程序
- **方案B**: 修改服务器端口
  1. 编辑 `server/src/main/java/edu/hitsz/server/BattleServer.java`
  2. 修改 `private static final int PORT = 8080;` 为其他端口（如8081）
  3. 重新编译：`gradlew.bat :server:build`
  4. 在客户端输入新的端口号

---

#### 3. 防火墙阻止连接
**症状**: 服务器启动成功，但客户端无法连接

**检查方法**:
```bash
# 测试本地连接
curl -X POST http://localhost:8080/battle/create -H "Content-Type: application/json" -d "{\"playerName\":\"Test\"}"
```

**解决方案**:
1. 打开 Windows Defender 防火墙
2. 点击"允许应用通过防火墙"
3. 找到 Java 或 javaw.exe
4. 确保"专用"和"公用"都勾选
5. 或者临时关闭防火墙测试

---

#### 4. 模拟器网络配置错误
**症状**: 真机可以连接，但模拟器无法连接

**原因**: Android模拟器有特殊的网络配置

**解决方案**:
- ✅ **正确**: 在模拟器中使用 `10.0.2.2`
- ❌ **错误**: 使用 `localhost` 或 `127.0.0.1`

**说明**:
- `10.0.2.2` 是模拟器访问宿主机的特殊IP
- `localhost` 在模拟器中指向模拟器自己，不是宿主机

---

#### 5. 客户端代码问题
**症状**: 服务器日志没有收到请求

**检查AndroidManifest.xml权限**:
```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
```

**检查网络安全配置**:
Android 9+ 默认不允许明文HTTP，需要配置：

创建 `app/src/main/res/xml/network_security_config.xml`:
```xml
<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
```

在 `AndroidManifest.xml` 中添加:
```xml
<application
    android:networkSecurityConfig="@xml/network_security_config"
    ...>
```

---

#### 6. Gson依赖问题
**症状**: 服务器启动失败，提示找不到Gson类

**解决方案**:
```bash
# 重新下载依赖
gradlew.bat :server:build --refresh-dependencies
```

---

## 调试步骤

### 步骤1：验证服务器
```bash
# 1. 启动服务器
双击 "启动服务器.bat"

# 2. 测试创建房间
curl -X POST http://localhost:8080/battle/create ^
  -H "Content-Type: application/json" ^
  -d "{\"playerName\":\"TestPlayer\"}"

# 应该返回类似：
# {"success":true,"roomId":"ROOM_1715400000000","playerId":"P1_1715400000000","message":"房间创建成功"}
```

### 步骤2：验证客户端网络
```bash
# 在Android Studio的Logcat中查看日志
adb logcat | findstr BattleClient

# 应该看到网络请求和响应
```

### 步骤3：检查服务器日志
服务器窗口应该显示：
```
[创建房间] ROOM_1715400000000 by TestPlayer
[加入房间] ROOM_1715400000000 by Player2
[对战结束] ROOM_1715400000000 | P1: 1500 vs P2: 1200
```

---

## 完整测试流程

### 1. 启动服务器
```bash
双击 "启动服务器.bat"
等待看到启动成功消息
```

### 2. 测试服务器（可选）
```bash
双击 "测试服务器.bat"
或使用浏览器访问 http://localhost:8080/battle/status?roomId=test&playerId=test
```

### 3. 启动模拟器
```bash
# 在Android Studio中启动两个模拟器
# 或使用命令行
emulator -avd Pixel_5_API_30
emulator -avd Pixel_5_API_30_2
```

### 4. 安装游戏
```bash
# 编译并安装
gradlew.bat installDebug

# 或在Android Studio中点击Run按钮
```

### 5. 测试联机
1. 模拟器1：创建房间
   - 服务器地址：`10.0.2.2`
   - 点击"创建房间"
   - 记录房间ID

2. 模拟器2：加入房间
   - 服务器地址：`10.0.2.2`
   - 输入房间ID
   - 点击"加入房间"

3. 开始对战
   - 两个玩家都点击"开始对战"
   - 观察分数同步

---

## 错误代码说明

| 错误代码 | 说明 | 解决方案 |
|---------|------|---------|
| 网络错误 | 无法连接到服务器 | 检查服务器是否启动，IP地址是否正确 |
| 房间不存在 | 房间ID错误或已过期 | 检查房间ID是否正确输入 |
| 房间已满 | 房间已有2名玩家 | 创建新房间或等待 |
| 无效的玩家ID | 玩家ID不匹配 | 重新加入房间 |

---

## 高级调试

### 查看详细网络日志
在 `BattleClient.java` 中添加日志：
```java
Log.d("BattleClient", "Request URL: " + request.url());
Log.d("BattleClient", "Request Body: " + requestBody);
Log.d("BattleClient", "Response: " + responseBody);
```

### 使用Postman测试API
1. 创建房间:
```
POST http://localhost:8080/battle/create
Content-Type: application/json

{
  "playerName": "TestPlayer"
}
```

2. 加入房间:
```
POST http://localhost:8080/battle/join
Content-Type: application/json

{
  "roomId": "ROOM_1715400000000",
  "playerName": "Player2"
}
```

---

## 联系支持

如果以上方法都无法解决问题，请提供：
1. 服务器启动日志
2. Android Logcat日志（过滤BattleClient）
3. 错误截图
4. 测试环境信息（操作系统、Java版本、Android版本）

---

## 快速检查清单

- [ ] 服务器已启动并显示成功消息
- [ ] 端口8080未被占用
- [ ] 防火墙允许Java程序
- [ ] 模拟器使用10.0.2.2地址
- [ ] AndroidManifest.xml有网络权限
- [ ] 游戏APK已安装到模拟器
- [ ] 两个模拟器都能正常运行游戏

全部勾选后，联机功能应该可以正常工作！
